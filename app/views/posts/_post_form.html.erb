<%= form_for @new_post, html: { multipart: true } do |f| %>

	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :picture %>
	<%= f.file_field :picture %>

	<%= f.submit 'Add post' %>

<% end %>