<%= form_for @new_post, html: { multipart: true } do |f| %>

	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :picture %>
	<%= f.file_field :picture %>

	<%= f.label :tag_list, 'Hashtag' %> 
	<%= f.text_field :tag_list %>

	<%= f.submit 'Add post' %>

<% end %>