<section class='add_post col-md-3'>

<%= bootstrap_form_for @new_post, html: { multipart: true } do |f| %>

	<%= f.text_field :title %>

	<%= f.text_field :tag_list %>

	<%= f.file_field :picture %>

	<%= f.submit 'Add post' %>

<% end %>

<section>

